# FlockTrax Demo BinSentry Feed Reports Checkpoint — 2026-09-08

## Checkpoint status

This is the authoritative continuation and recovery note for the simulated BinSentry feed-report work on the isolated hosted demo. The governing rule remains **DO NO HARM**: production data, production credentials, the production Supabase project, and the production Vercel deployment were not changed.

At this checkpoint, the demo database work is live and verified. The shared web application changes are committed and pushed, but their Vercel production deployment is pending because the Vercel account entered two-factor account recovery and is under review.

Checkpoint recorded at `2026-09-08 10:54 CDT`.

## Git restore boundary

- Worktree: `C:\dev\FlockTrax-Demo-Hosted`
- Branch: `demo-hosted`
- Remote branch: `origin/demo-hosted`
- Feed-report implementation commit: `9d1f622` — `Simulate BinSentry feed reports in demo`
- Feed-prediction seed commit: `4c44b46` — `Seed demo feed prediction inputs`
- Detailed checkpoint tag: `checkpoint/demo-hosted-feed-reports-20260908`
- Production `origin/main` at checkpoint time: `4b494db0ab097eed3e4f748163da3670820c2aeb`
- Existing production fallback tag `pre-demo-stable-2026-09-05`: `7c7d5d6cc7265436593df6375db8bd04711bdf3f`

After this note and its index entry are committed, the checkpoint tag is placed on that documentation commit. To inspect or restore it without moving either active checkout:

```powershell
git -C C:\dev\FlockTrax worktree add C:\dev\FlockTrax-Demo-Feed-Restore checkpoint/demo-hosted-feed-reports-20260908
```

Do not use a destructive reset against the production checkout.

## Environment boundary

- Demo application: `https://flocktrax-demo.vercel.app`
- Demo Vercel project: `flock-trax/flocktrax-demo`
- Demo Vercel project id: `prj_kd5UF1Bs69QRjICqxwdyiIiXvRNY`
- Demo Supabase project ref: `srkgobayrzidytmvoago`
- Production Supabase project ref: `frneaccbbrijpolcesjm`
- Demo outbound mode: `disabled`
- Demo owner: `ken@mothercluckershenhouse.com`
- Demo owner user id: `ac1ee85e-7ed3-4ee0-adc2-59d5fea0b12a`

The demo application still shares the production application source, but its runtime is bound to the separate demo Supabase project. Environment guards reject a demo build bound to the protected production project or hostname. No BinSentry, Google Sheets, SMTP, Adalo, or other production integration credential was added to the demo.

## Implemented behavior

The demo now simulates only the external BinSentry supply inputs. The FlockTrax demand calculation remains real and reads current demo operational records each time the report is loaded.

Demand inputs remain live within the demo database:

- active and scheduled placements;
- starting male and female head counts;
- all active mortality and cull records;
- recent mortality rates used for forward loss projections;
- breed and sex-specific daily feed-consumption curves;
- current and future livehaul schedule records;
- livehaul actual head when recorded, otherwise target head;
- delivered starter and grower feed history.

Simulated supply inputs are isolated to:

- latest per-bin inventory snapshots;
- accessible and queued feed types and pounds;
- pending and partially fulfilled BinSentry-style orders;
- relative current timestamps and delivery dates regenerated by reset.

Production continues to execute the existing live BinSentry API path. Demo mode selects the internal database-backed provider before authentication or vendor HTTP calls occur.

## Livehaul and mortality behavior

The projection report reads `public.livehaul_schedule` and current mortality rows on every report load. A demo user can add or edit mortality and livehaul records, refresh the report, and observe a changed feed requirement.

The source correction in commits `9d1f622`/`4c44b46` changes livehaul precedence from `targetHead ?? actualHead` to `actualHead ?? targetHead` in both feed-projection implementations. Completed or updated events therefore use the recorded actual head removal when available and retain the target only as a fallback.

The simulated BinSentry layer does not freeze or replace these operational inputs.

## Database migrations applied to demo

These migrations were applied successfully through the Supabase migration tooling while the local project link resolved to `srkgobayrzidytmvoago`:

1. `20260908190000_seed_demo_binsentry_feed_reports.sql`
   - creates the guarded `demo_control.ensure_binsentry_feed_showcase()` helper;
   - refreshes five synthetic feed-bin readings;
   - creates three `source='binsentry_demo'` open/partial commitments;
   - extends the guarded reset and machine-readable status path.
2. `20260908191000_seed_demo_feed_prediction_curves.sql`
   - creates two demo-only Cobb 500 breed rows;
   - creates female and male feed/weight curves for ages 0 through 90;
   - assigns those breeds to all five deterministic demo flocks;
   - composes breed-curve recreation into every reset.
3. `20260908192000_seed_demo_binsentry_inventory_snapshots.sql`
   - creates five `source='binsentry_demo'` inventory snapshots;
   - makes the projection engine's existing snapshot-based on-hand calculation operational;
   - composes snapshot recreation into every reset.

Each helper checks the private demo marker, expected project ref, and expected owner before changing data. The public reset wrapper remains executable only by `service_role`; ordinary anonymous and authenticated callers cannot invoke it directly.

The migrations were applied without running a full showcase reset. Existing current mortality, livehaul, daily-log, placement, and other user-test records were preserved. Only deterministic demo feed snapshots, breed curves/assignments, and `binsentry_demo` orders were created or refreshed.

## Reset composition

The guarded reset now rebuilds the baseline in this order:

1. `demo_control.reset_showcase_data()`
2. `demo_control.ensure_multi_group_showcase()`
3. `demo_control.ensure_binsentry_feed_showcase()`
4. `demo_control.ensure_feed_prediction_curves()`
5. `demo_control.ensure_binsentry_inventory_snapshots()`
6. `demo_control.full_showcase_status()`

Resetting reanchors transaction dates, snapshot timestamps, and pending delivery dates to the current date. It also restores 91 feed-curve days for both sexes, five inventory snapshots, three pending simulated orders, the two farm groups, and the original operational showcase dataset.

## Synthetic feed scenarios

- Cedar Creek A-1:
  - Starter bin: 8,600 lbs
  - Grower bin: 17,200 lbs
  - Scheduled starter order: 16,000 lbs
- Cedar Creek A-2:
  - Grower inventory: 6,100 lbs
  - Partially fulfilled grower order: 22,000 ordered, 4,000 received, 18,000 remaining
- Redbud Ridge B-1:
  - Grower inventory: 11,900 lbs
  - Scheduled grower order: 12,000 lbs
  - Upcoming livehaul remains an active demand adjustment
- Redbud Ridge B-2:
  - Empty synthetic bin for the future scheduled placement

Order references are synthetic (`BS-DEMO-4107`, `BS-DEMO-4112`, and `BS-DEMO-4120`). No vendor system was contacted.

## Verification evidence

Completed successfully before this checkpoint:

- `npm run typecheck`
- optimized `npm run build`, including all 49 application pages/routes
- `git diff --check`
- all three new migrations applied successfully to the linked demo Supabase project
- live hosted report read-back after database deployment
- live demand calculation changed from zero to populated values after breed assignment
- live on-hand calculation changed from zero to populated values after inventory-snapshot seeding
- current hosted report values at the verification instant:
  - three barns in scope;
  - 136,107 lbs ten-day projected demand;
  - 43,800 lbs on hand;
  - 46,000 lbs remaining across three simulated orders;
  - 77,599 lbs overall recommended order;
  - non-zero daily demand across all ten displayed dates.
- barn-level rows showed distinct inventory/order/recommendation scenarios
- both Google/platform outbound queues remained outside this work and outbound mode remained disabled
- Git branch pushed successfully through commit `4c44b46`

The values above are a point-in-time verification, not hard-coded report totals. Mortality, livehaul, placement, date, inventory, order, and curve changes will change them.

## Web application changes awaiting Vercel deployment

The committed web changes:

- select the database-backed simulated provider only when `FLOCKTRAX_ENVIRONMENT_NAME=demo`;
- bypass all live BinSentry authentication and HTTP reads in demo mode;
- keep production on the existing live API provider;
- prevent `binsentry_demo` commitments from being counted once as FlockTrax orders and again as simulated BinSentry orders;
- include simulated typed orders in typed-ordering availability;
- show `BinSentry (simulated)` as the source;
- display a clear notice that supply data is simulated while forecast calculations use current operational records;
- make Current Feed Inventory read demo snapshots rather than calling the vendor;
- prefer actual livehaul head over target head in both calculation implementations;
- show BinSentry bin/order and feed-curve counts on Environment Control.

The database-backed calculations are already visible through the previously deployed web build, but that older build labels the commitments as `FlockTrax (Binsentry Demo)` and does not show the new explanatory banner. The committed build will correct that presentation after deployment.

## Vercel deployment blocker

The direct production deployment command failed safely because no local Vercel CLI credential was available. Browser login through the linked GitHub identity reached Vercel's pre-existing authenticator challenge. The owner did not recognize the authenticator setup and initiated Vercel account recovery. Vercel reports that recovery is under review.

No duplicate Vercel account or project was created. The existing public demo deployment remains online and points only to the isolated demo Supabase project.

After Vercel restores account access:

1. Sign in to the existing Vercel account/team.
2. From `C:\dev\FlockTrax-Demo-Hosted\web-admin`, authenticate the Vercel CLI if needed.
3. Confirm `.vercel/project.json` still names project `flocktrax-demo` and project id `prj_kd5UF1Bs69QRjICqxwdyiIiXvRNY`.
4. Run:

```powershell
npx --yes vercel@latest deploy --prod --yes
```

5. Verify the deployment aliases to `https://flocktrax-demo.vercel.app`.
6. Sign in to the demo and verify:
   - `/admin/reports/feed-projection?includeBinSentryOnOrder=1`
   - `/admin/reports/feed-projection-custom?includeBinSentryOnOrder=1`
   - `/admin/reports/feed-inventory?includeBinSentryOnOrder=1`
7. Confirm the simulated-data notice appears, the order source reads `BinSentry (simulated)`, inventory/demand totals are non-zero, and browser/server logs contain no BinSentry authentication or vendor request failure.

## Resume instruction

Resume only from `C:\dev\FlockTrax-Demo-Hosted` on branch `demo-hosted`. Before any database mutation, verify `supabase/.temp/project-ref` equals `srkgobayrzidytmvoago`. Preserve `origin/main`, the production checkout, and the existing production fallback tag. Complete the Vercel deployment only into project `flocktrax-demo`; do not attach the production domain, connect production credentials, or create a replacement Vercel account/project merely to avoid the recovery process.
